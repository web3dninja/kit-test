# Firebase Configuration
# NOTE: All Firebase config variables must be NEXT_PUBLIC_* because they are used in client-side code
# This is safe - Firebase config is designed to be public
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id

# JWT Secret Key (SERVER-ONLY - never use NEXT_PUBLIC_ prefix!)
# This secret is only available on the server and should NEVER be exposed to the client
# Generate with: pnpm generate-jwt-secret
JWT_SECRET=your_jwt_secret_key_here
